 ```yaml
name: Futures Daily Analysis on: schedule: - cron: ‘25 7 * * 1-5’ # 北京时间 15:25，周一到周五 workflow_dispatch: # 手动触发 jobs: run: runs-on: ubuntu-latest steps: - uses: actions/checkout@v3 - name: Set up Python uses: actions/setup-python@v4 with: python-version: ‘3.11’ - name: Install dependencies run: | python -m pip install --upgrade pip pip install akshare pandas requests - name: Run futures analyzer env: PYTHONIOENCODING: utf-8 run: | python main.py --once
